import {GetTeamsList} from "../teamsList";
import {getTeams} from "../../fetchApi/apiData";


export const getTeamById = (list,id) => {
 // console.log(list['id'])
 for (let team in list){
  if(list[team].team_id){

  }
 }
 return (list['id']===id)
   // console.log(team)

}